version: '3.8'

services:
  amazon-tracker-bot:
    build: .
    container_name: amazon-tracker-bot
    env_file:
      - .env
    environment:
      DB_PATH: /data/tracker.db
    volumes:
      - amazon_tracker_data:/data
    restart: unless-stopped

volumes:
  amazon_tracker_data:
